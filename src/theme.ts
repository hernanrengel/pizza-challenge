import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#D32F2F', // red
    },
    secondary: {
      main: '#FFA000', // orange
    },
    background: {
      default: '#EFEFEF',
    },
  },
});
